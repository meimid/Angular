export class Account {	
	  public libelle:string='';
       public numAccount:string='';
      public constructor(numCompte:string ,lib:string) {
		  this.libelle=lib;
		  this.numAccount=numCompte;
		  
		  
	  }
}
